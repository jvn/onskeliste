<h1>New wish</h1>
<% @wish.event = @event %>
<%= form_for(@wish) do |f| %>
  <% if @wish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wish.errors.count, "error") %> prohibited this wish from being saved:</h2>

      <ul>
      <% @wish.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :wish %><br />
    <%= f.text_field :wish %>
  </div>
  <div class="field">
    <%= f.label :talen %><br />
    <%= f.number_field :talen %>
  </div>
  <div class="actions">
    <%= f.submit(:id=>@event.id) %>
  </div>
<% end %>

<%= link_to 'Back', wishes_path(:id=>@event.id) %>
t