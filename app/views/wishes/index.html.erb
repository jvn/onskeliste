<h1>Listing wishes</h1>

<table>
  <tr>
    <th>Event</th>
    <th>Wish</th>
    <th>Talen</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @wishes.each do |wish| %>
  <tr>
    <td><%= wish.event_id %></td>
    <td><%= wish.wish %></td>
    <td><%= wish.talen %></td>
    <td><%= link_to 'Show', wish %></td>
    <td><%= link_to 'Edit', edit_wish_path(wish) %></td>
    <td><%= link_to 'Destroy', wish, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Wish', new_wish_path %>
