<div class="container">
    <p id="notice"><%= notice %></p>

    <p>
        <center style="padding-top: 40px;">
            <b>Welkomen</b> <%= @user.username %><a href="http://localhost:3000"></a><br/>
        </center>
    </p>

    <div title="Half and half" class="row show-grid">
        <div class="span8">
            <center><b>Dine Begivenheder</b></center>

            <table>
                <tr>
                    <th>Begivenhed</th>
                    <th>dato</th>
                    <th></th>
                </tr>

                <% @myevents.each do |event| %>
                    <tr>
                        <td><%= event.Event_Name %></td>
                        <td><%= event.date %></td>
                        <td><%= link_to 'Ret', wishes_path(:id=>event.id) %></td>
                    </tr>
                <% end %>
            </table>

            <%= link_to 'Tilføj', new_event_path(:id=>@user.id) %>
        </div>

        <div class="span8">
            <center><b>Dine venners Begivenheder</b></center>

            <table>
                <tr>
                    <th>Begivenhed</th>
                    <th>dato</th>
                    <th></th>
                </tr>

                <% @myevents.each do |event| %>
                    <tr>
                         <td><%= event.Event_Name %></td>
                         <td><%= event.date %></td>
                         <td><%= link_to 'Ret', wishes_path(:id=>event.id) %></td>
                    </tr>
                <% end %>
            </table>
        </div>
    </div>

</div>
